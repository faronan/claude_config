---
description: ステージ済み変更からコミットメッセージを生成してコミット
---

1. `git diff --staged` で変更内容を確認
2. commit-message スキルのルールに従ってメッセージを生成
3. 生成したメッセージを表示して確認を求める
4. 承認されたら `git commit -m "..."` を実行

$ARGUMENTS があれば追加のコンテキストとして使用。
