# Cursor Skills

`bin/install.sh` links `~/.cursor/skills` to this repository's canonical
`home/.agents/skills` directory.

Keep skill implementations in `home/.agents/skills` so Cursor and Codex share
one source of truth and do not drift.
